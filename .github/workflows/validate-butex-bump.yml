name: Validate BuTeX bump

on:
  push:
    branches:
      - chore/bump-butex-7.1.1

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        working-directory: frontend
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: BuTeX-focused tests
        working-directory: frontend
        run: npx vitest run src/lib/butex-images.test.ts src/lib/butex-validation.test.ts src/lib/mapping-fonts.test.ts
      - name: Typecheck
        working-directory: frontend
        run: npx tsc --noEmit
      - name: Build
        working-directory: frontend
        run: npm run build
